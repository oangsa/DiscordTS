export default interface IUser {
    id: string;
    discordId: string;
    username: string;
    globalName?: string;
    avatarUrl?: string;
    createdAt: string;
    updatedAt: string;
}
